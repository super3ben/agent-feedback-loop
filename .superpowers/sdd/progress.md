# Immediate Subagent Reflection SDD Ledger

- Branch: `codex/isolate-feedback-control-plane`
- Plan: `docs/superpowers/plans/2026-07-16-immediate-subagent-reflection.md`
- Baseline: 216 tests, 215 passed; one obsolete Stop timing test failed once and passed on isolated rerun
- Review policy: thorough review with a three-round circuit breaker; after three cumulative fix/formal-review rounds, architecture retrospective and a frozen acceptance checklist replace open-ended review. Only main-session interference, data corruption/unrecoverability, security/privacy, or frozen-core failure blocks; unsupported theoretical edge cases enter backlog. Review Loop Guard applies from the current Task 7 frozen review and all later tasks; Task 1 stays frozen and Tasks 2–6 receive no historical backfill
- Guard source of truth: `.superpowers/sdd/review-loop-state.json`; every review has a stable Review-Run-ID, every Critical/Important finding has a stable Invariant-ID and Boundary, and fixes require record-review -> authorize-fix -> persisted receipt -> receipt-consuming fix brief. Guard exits 3/4/5/6 are hard stops; aliases/distinct findings use the Guard commands, never replacement IDs
- Safety boundary: temporary HOME/DB copies only; real hooks, runtime pointer and database stay untouched

| Task | Status | Implementation | Review | Verification |
| --- | --- | --- | --- | --- |
| 1. Lean control DB | complete (frozen acceptance) | `4a1791a`, `aa770c6`, `864240b`, `9e62862`, `44acbfd`, `5053dda`, `d11cb8a`, `535704d`, `da19db1`, `9fb6cd6`, `88c2c4b`, `d37c91f`; design `e1732a8`; plan `c6f984f`, circuit breaker `d3dd12a` | PASS A-G; Critical 0; backlog 0 | Timestamp RED 0/2; UTC and Asia/Shanghai GREEN 2/2 each; disposable-HOME four-file regression 159/159; scope two files. macOS temporary-state build proof only; Linux/live/production remain unverified |
| 2. Immediate job and fenced lease APIs | complete | `12275a6`, fix `06c4389` | APPROVED; Critical 0, Important 0, backlog 0 | fix RED 0/1; fix GREEN 1/1; Task 2 focused 9/9; control/store/capture 160/160; coordinator disposable reprobe and fresh re-review close exhaustion |
| 3. Remove Stop/notification/reconcile | complete (frozen) | `7f1756d`; fix `d96e77e` | APPROVED; Critical 0, Important 0, backlog 0; A-G PASS | initial RED 0/3 and 27/27; fix RED 0/2, GREEN 2/2; coordinator 28/28; reviewer focused 2/2 plus bounded/idempotent cleanup probes |
| 4. Explicit dissatisfaction detector | complete (frozen) | `3cfeada` | APPROVED; Critical 0, Important 0, backlog 1; A-F PASS | main RED 0/1 plus boundary RED 0/2; capture 35/35; coordinator 170/170; reviewer 9 assertions |
| 5. Prompt capture creates immediate job | complete (frozen) | `fda09b7` | APPROVED; Critical 0, Important 0, Minor 0, backlog 0; A-G PASS | required RED 0/4, boundary RED 0/5; implementer/coordinator five-file regression 195/195; reviewer two disposable probes passed; four-file scope |
| 6. macOS/Linux detached launcher | complete (frozen) | `dd18ed3` | APPROVED; Critical 0, Important 0, backlog 0; A-G PASS | RED 8 pass/1 missing-module fail; focused 16/16; Task 2/5/6 regression 111/111; reviewer 7/7; real disposable macOS detached lifecycle passed; Linux live proof deferred to Task 15 |
| 7. Reviewer result contract | complete (frozen) | `63cc5b6`; fix `25ebbb5` | APPROVED; Critical 0, Important 0, Minor 0; Guard loop closed | initial focused 24/24 and combined 42/42; fix RED 0/1, GREEN 1/1, combined 42/42; coordinator 11/11 plus secret-shaped probe; real disposable macOS FIFO proof |
| 8. Canonical/legacy Markdown documents | complete (frozen) | `a3b34c4`; fix `ab54c8c` | APPROVED; Critical 0, Important 0, Minor 0; all three Guard loops closed | initial 11/11; fix 14/14; native macOS roundtrip/no-overwrite/path-confinement/0600/hash/catalog/adoption proof |
| 9. Reviewer runner terminal outcomes | complete (frozen) | `1497203`; fix `f1bc209` | APPROVED; Critical 0, Important 0, Minor 0, Backlog 0; both Guard loops closed | initial RED 93/98; fix RED 7/12; focused 147/147; full 302/302; fresh reviewer 5/5; native macOS detached proof |
| 10. Deterministic Markdown Top-K | complete (frozen) | `7d927bb`, test closeout `11e0cb9`, fix `41c7ea7` | APPROVED; Critical 0, Important 0, Minor 0; Guard loop closed | initial RED 30/43; fix RED 0/1; focused+store 100/100; full 301/301; native macOS proof |
| 11. Selected/emitted/recurrence evidence | complete (frozen) | `41e32fd` | APPROVED; Critical 0, Important 0, Minor 0, Backlog 0; no Guard finding opened | RED 0/5 plus 0/3; focused 8/8; affected 131/131; full 309/309; reviewer novel probe passed |
| 12. Read-only legacy export | complete (frozen) | `049c598`, fix `8954176` | APPROVED; Critical 0, Important 0, Minor 0, Backlog 0; Guard loop closed | focused 62/62; full 333/333; native disposable macOS CLI proof |
| 13. Delete transitional legacy runtime | complete (frozen) | `d055993`; fix `f8c9773` | APPROVED; Critical 0, Important 0, Minor 0, Backlog 0; both Guard loops closed | RED import/runtime boundary; focused 3/3; affected 143/143; E2E 9/9; full 258/258; native disposable macOS doctor proof |
| 14. Doctor/logs/docs/package | complete (frozen) | `5451799`; fix `d14731c` | APPROVED; Critical 0, Important 0, Minor 0, Backlog 0; four Guard loops closed | initial 48/48; fix targeted 50/50; full 264/264; pack 0.8.0/31 files; native disposable macOS install + doctor --live ready |
| 15. End-to-end macOS/Linux build proof | in progress (preflight) | base `cb4ec7a` | pending | macOS Node v26/Codex 0.134 and Docker Linux engine available; guarded real-state before snapshot recorded read-only |
| Final branch review | pending | pending | pending | pending |

## Notes

- Every implementation or fix agent must report an observed RED command and failure summary before GREEN evidence.
- Every task receives review according to the circuit-breaker policy above. Before the threshold, frozen-scope Critical/Important findings are fixed and re-reviewed; after the threshold, adjacent non-Critical findings cannot extend the loop.
- Task 1 architecture retrospective: the core control DB direction is valid, but compatibility responsibilities and the review gate became overextended while the user-facing vertical loop remained unbuilt. The timezone-less counterexample has no supported producer evidence; one minimal normalization closeout is cheaper than more control-store redesign, after which Tasks 2–6 take priority.
- Task 1 frozen acceptance: separate schema-v1 control DB and legacy isolation; side-effect-free frozen canonical preflight; blob/transaction/ref invariants; exact/alias/new replay and completeness regressions; timezone-less pre-side-effect rejection; timezone-bearing UTC-normalized alias replay; no real-state mutation.
- Frozen timestamp closeout commit `d37c91f` changes only `src/control-store.mjs` and `test/control-store.test.mjs`. It adds exactly two tests and one small entry normalizer; SQL/schema/fallback behavior is unchanged. Reported GREEN is 2/2 under both `TZ=UTC` and `TZ=Asia/Shanghai`, plus 159/159 four-file regression.
- Frozen Task 1 acceptance passed checklist A-G with Critical 0 and backlog 0. Task 1 is frozen and complete; no twelfth open-ended review/fix round is permitted. Linux, live hooks/runtime, Codex desktop visibility, and production evidence remain later verification boundaries.
- Task 2 preflight: the schema-v1 `reviewer_jobs` and `review_job_events` tables already contain the needed identity, launch, lease, retry and terminal fields. The bounded task adds only transactional control-store APIs and focused tests; it does not add a scheduler, notification path, RAG layer, schema change, Markdown body storage, or main-session wait.
- Task 2 implementation commit `12275a6` changes only `src/control-store.mjs` and `test/control-store.test.mjs`; reported focused GREEN is 8/8 and required three-file regression is 159/159. A coordinator disposable-DB probe found one frozen-core failure: after the third claimed worker crashes and its lease expires, recovery returns no job while launch reservation reports terminal, leaving the row indefinitely `running` at attempt 3. The task remains unchecked pending a frozen-scope review and correction.
- Task 2 frozen review 1 returned `CHANGES_REQUIRED` with Critical 0, Important 1 and backlog 0. It independently reproduced only the attempt-3 crash exhaustion failure. The authorized fix is limited to making the existing bounded `listRecoverableReviewJobs` → `reserveReviewLaunch` path persist one fenced `failed/attempts_exhausted` terminal transition and adding one regression; no new API or adjacent scope is allowed.
- Task 2 bounded fix commit `06c4389` changes only the two owned files. It exposes the due expired third attempt to bounded recovery and atomically terminalizes it during reservation; the new regression is 1/1, Task 2 focused tests are 9/9, and the required three-file regression is 160/160. A separate coordinator disposable-DB reprobe observed one due job followed by persisted `failed`, cleared lease/retry/launch fields and exactly one `attempts_exhausted` event.
- Task 2 frozen re-review returned `APPROVED`: Critical 0, Important 0, backlog 0; I1 is closed and checklist A-G has no regression. Evidence remains local macOS Node/SQLite with disposable state; Linux, detached process, provider, live hooks, installation, production and true multi-process stress remain later verification boundaries.
- Task 3 preflight: obsolete runtime ownership is concentrated in `src/index.mjs`, `src/cli.mjs`, `src/codex-host.mjs`, two hook templates and the three notification/reconcile modules named by the plan. The change deletes those active entrypoints and tests while retaining the isolated legacy store/schema/receipt modules until Task 13; upgrade fixtures must preserve unrelated user Stop hooks. No real install or global hook mutation is authorized.
- Task 3 implementation commit `7f1756d` is a net deletion limited to the 16 task-owned files; reported RED is 0/3, final focused GREEN 27/27 and the static control-symbol gate has no matches. A coordinator disposable-HOME probe found one frozen-core upgrade failure: if an unmarked Codex `[[hooks.Stop]]` group contains both the legacy AFL handler and an unrelated user handler, `removeUnmarkedLegacyCodexBlocks()` drops the whole group and loses the user hook. Task 3 remains unchecked pending frozen-scope review and correction.
- Task 3 frozen review 1 returned `CHANGES_REQUIRED` with Critical 0, Important 2 and backlog 0. In addition to marked/unmarked mixed Codex groups losing unrelated nested handlers, upgrade/uninstall leaves the old `io.github.super3ben.agent-feedback-loop.reconcile.plist` KeepAlive job/file behind. The bounded fix is handler-level TOML cleanup plus idempotent one-time legacy launchd bootout/unlink through an injected fake host in tests; it must not restore scheduler paths, commands, modules or health APIs.
- Task 3 bounded fix commit `d96e77e` changes only `src/index.mjs` and `test/cli.test.mjs`. It removes only AFL-owned nested Codex handlers while retaining mixed-parent matcher/options and user siblings, and performs private macOS-only best-effort cleanup of the exact legacy LaunchAgent label/plist without restoring a scheduler surface. Focused RED was 0/2, GREEN 2/2, and both implementer and coordinator Task 3 suites passed 28/28; static, syntax, shell, diff and scope gates passed. Evidence uses disposable HOME and a fake bootout host only; real launchd/global config, Linux and production remain unverified pending later tasks.
- Task 3 frozen re-review returned `APPROVED`: Critical 0, Important 0, backlog 0; I1/I2 closed and checklist A-G passed. Independent evidence was focused 2/2 plus a 5005ms hanging fake-bootout bound, retry idempotency, non-Darwin and symlink isolation probes. Task 3 is frozen complete; no additional review round is allowed. Linux-host, real launchd/config/runtime/UI and production evidence remain later verification boundaries.
- Post-review macOS cleanup proof: a read-only machine audit found the old exact reconcile label still loaded from the disposable `/tmp/afl-task3-machine.VuWihM` fixture created on 2026-07-16 (PID 13938), not from the real user HOME. Running the current `uninstall()` against that exact temporary HOME with legacy cleanup activation removed the plist and runtime, booted the job to `SIGTERMed`, then the label and PID both disappeared. This is real macOS launchd cleanup evidence for a disposable fixture, not production/user-HOME proof.
- Task 4 preflight: add one pure local, explainable detector and reuse the existing owned/non-symlink/bounded transcript-tail primitive. The required completed-turn Chinese dissatisfaction must emit the four frozen reason codes; neutral AFL questions, invited design choices and single keywords remain negative. Stable host ids win; replay-safe fallback hashes session/turn/transcript/content identity and reports unstable identity instead of inventing idempotency. Task 4 does not create reviewer jobs, detach processes, publish/read Markdown, or touch live state.
- Task 4 implementation commit `3cfeada` changes exactly `src/feedback-signal.mjs`, `src/capture.mjs` and `test/capture.test.mjs`. The exact dissatisfaction and two coordinator-requested contract checks were observed RED (0/1 and 0/2), then the final capture suite passed 35/35; coordinator also ran capture/control-store/legacy-store regression 170/170. The implementation is pure local classification, role-validated bounded referent reading and deterministic identity normalization, with no job/launch/provider/Markdown/schema/control-output side effects or content logging. Evidence is local macOS temporary-state only; Linux/live/production remain unverified.
- Task 4 frozen review returned `APPROVED`: Critical 0, Important 0, backlog 1; checklist A-F passed and an independent nine-assertion probe verified the core positive/no-referent/synthetic/identity boundary. The only backlog item is a native explicit-feedback event mapping with no supported producer contract or production evidence; it does not block and cannot start a Task 4 patch round. Task 4 is frozen complete.
- Task 5 preflight: prompt-only orchestration must fix `selectionPublishedBefore` before any candidate work, create/capture evidence only for a stable real candidate, transactionally create the source-identity job before reserving or synchronously calling the injected launcher, and always write a native host no-op without AFL operational text. Replay reuses one job/reservation cooldown; same complaint in another session creates another job; ordinary, unstable-identity and failure paths create no false job and remain host-pass. The normal CLI uses only a silent `runner_transition` no-op until Tasks 6/9 switch the actual process protocol; no embedded legacy launcher, receipt, threshold, scheduler, wait or status output may return.
- Task 5 implementation commit `fda09b7` changes exactly the four owned files. Required orchestration RED was 0/4 and the never-resolving/unstable/source-only structural/no-referent/installed boundary RED was 0/5; final five-file regression passed 195/195 for both implementer and coordinator. The handler freezes cutoff first, detects, captures encrypted candidate evidence, creates the durable job, reserves, invokes but never awaits the synchronous launcher, isolates optional read-only legacy selection and always writes a native no-op. The installed temporary-HOME e2e creates one pending launch-epoch-1 job without stdout/stderr control text. Real detached spawn/recovery remains Task 6/9 and no real user state was touched.
- Task 5 frozen review returned `APPROVED`: Critical 0, Important 0, Minor 0, backlog 0; checklist A-G passed. Two disposable probes confirmed a declared Claude referent without a timestamp still creates one job/launch and identical complaint text in a later turn of the same session creates a distinct second job/launch. Task 5 is frozen complete; Task 6/9/10 boundaries remain excluded and no additional Task 5 review round is allowed.
- Task 6 preflight: keep the process primitive independent of SQLite; `launchDetachedReviewer` validates and performs only direct synchronous `spawn`/`unref`, while `recoverDueReviewers` owns stable due listing, launch reservation and exact-epoch synchronous-failure release. Recovery is hard-capped at one per prompt and is not a scheduler. Frozen A-G covers macOS/Linux platform/options, scrubbed environment, no inherited output, fail-open recovery, real disposable macOS parent/child lifecycle, and exclusion of legacy runner/production wiring/schema/live state. Baseline `test/e2e-smoke.test.mjs` passed 8/8 on Darwin arm64 with Node v26.0.0; actual Linux execution remains Task 15 evidence.
- Task 6 implementation commit `dd18ed3` changes exactly the three owned files. RED was 8 pass / 1 expected `ERR_MODULE_NOT_FOUND`; focused GREEN passed 16/16 and the wider launcher/e2e/CLI/control-store regression passed 111/111. The 152-line process module directly spawns and unrefs only on macOS/Linux, scrubs its environment, suppresses child output, consumes asynchronous child errors and returns bounded reasons; recovery lists/reserves/attempts at most one job and releases only matching-epoch synchronous failures. A disposable real macOS parent exited before its detached child wrote the delayed sentinel, with no child stdout/stderr in parent output. Production prompt wiring/new runner activation remains Task 9, and no real user state or Linux host was touched.
- Task 6 frozen review returned `APPROVED`: Critical 0, Important 0, backlog 0; checklist A-G passed and the reviewer independently ran the seven launcher tests without repeating the 111-test regression. It confirmed the implementation is proportionate and does not activate any scheduler, legacy runner or production prompt path. Task 6 is frozen complete; actual Linux, live hooks/provider/UI and production evidence remain Task 15 or later explicit verification boundaries.
- Task 7 preflight: define only the untrusted reviewer-result boundary before Task 9 activates it. The JSON Schema constrains provider generation, the standalone validator independently enforces exact fields/bounds/catalog/credential semantics, and the secure reader consumes one private no-follow result file then removes only that same owned regular file. Existing-family recurrence ids are prior reflection ids mapped by a controller-supplied `Map`; new families cannot claim recurrence and use one deterministic derived id. No database, Markdown publication, provider/runner/CLI wiring or real state changes are in scope. The unchanged legacy reviewer/provider/adapter/auth baseline passed 18/18 on Darwin arm64 with Node v26.0.0.
- Task 7 implementation commit `63cc5b6` changes exactly the five owned files. Two independent RED test tasks each failed solely on its missing module; focused GREEN passed 24/24 and the combined new plus unchanged reviewer runtime regression passed 42/42. The implementation is 195 lines of result validation, 91 lines of secure file handling and one 153-line provider schema; it adds no service or runtime wiring. A real disposable macOS FIFO was rejected nonblocking in 0ms and preserved. Syntax, JSON parse, diff, staged scope and Task 1-6 report hash gates passed; no real user state was touched.
- Task 7 frozen review `isolate-feedback-control-plane-task-7-v2-review-1` returned CHANGES_REQUIRED with one Important finding: invariant `T7-D-SECRET-PAYLOAD-REJECTION` at boundary `reviewer-result.semantic-validation.proposed-family-key`. Guard `record-review` and generation-1 `local_fix` authorization both returned exit 0; the persisted receipt was consumed by `.superpowers/sdd/isolate-feedback-control-plane-task-7-v2-fix-1-brief.md` before the single bounded fixer was dispatched. No Task 1–6 Guard events were backfilled.
- Task 7 fix commit `25ebbb5` routes grammar-valid `proposed_family_key` through the existing JavaScript secret/control scanner and adds the exact `sk-proj-...` regression. The fixer observed RED 0/1 then GREEN 1/1, 11/11 focused and 42/42 combined; the coordinator independently observed 11/11 and rejection of the reproduced payload. Guarded re-review `isolate-feedback-control-plane-task-7-v2-rereview-1` returned APPROVED with Critical 0, Important 0 and Minor 0, and `record-review --verdict approved` closed the original invariant without resetting its historical failure count.
- Task 8 preflight used three read-only real repository reports to confirm the supported legacy producer shapes: modern English named sections, Chinese named-section aliases, and older unbulleted metadata plus `Preventive Constraint`. Fixtures must remain synthetic/redacted and structurally match only those evidenced formats. Frozen A-F covers: (A) exact canonical validation/render/roundtrip with controller-derived identity/time/family fields; (B) explicit legacy aliases and exact normalized-class derivation, with incomplete documents omitted; (C) deterministic regular-`*.md` catalog reads with symlink, size/count and strict cutoff enforcement; (D) same-directory 0600 write/fsync/fenced-rename/directory-sync/hash verification with identical retry adoption and collision refusal; (E) exactly one owned non-symlink project/reflection root; (F) only the five declared files, with no DB, selector, runner, provider, hook, RAG, scheduler or real-state changes. The source envelope is `{sourceIdentity, createdAt, publishedAt}` so the controller, not this document layer, owns the fenced time and crash-retry reuse.
- Task 8 implementation commit `a3b34c4` changes exactly the five declared files; implementer and coordinator each observed 11/11 focused tests, and the implementer recorded a disposable native macOS 0600/hash/catalog/adoption smoke. Frozen review `isolate-feedback-control-plane-task-8-v2-review-1` found three independent blockers: Critical `publication-never-overwrites-conflict` at `reflection-document/atomic-publication`, Important `canonical-markdown-roundtrip-lossless` at `reflection-document/canonical-format`, and Important `publication-root-path-is-symlink-free` at `reflection-document/path-confinement`. Each Guard `record-review` and generation-1 `local_fix` authorization returned exit 0, and each receipt was consumed into a separate Task 8 fix brief before one serial fixer dispatch; no alias/distinct declaration was needed because the boundaries differ.
- Task 8 fix commit `ab54c8c` adds one reversible canonical text codec, replaces replace-existing rename with same-directory atomic no-replace publication, and verifies every explicit root path component. The fixer observed all three RED then 14/14 GREEN and a disposable native macOS proof; the coordinator repeated 14/14 plus the original supported-input/collision/symlink probes with lossless roundtrip, preserved competing target, symlink rejection and zero outside writes. Guarded re-review `isolate-feedback-control-plane-task-8-v2-rereview-1` returned APPROVED with Critical 0, Important 0 and Minor 0; three `record-review --verdict approved` calls closed the original loops without resetting their failure history. Task 8 is frozen complete.
- Task 9 preflight freezes a single vertical runtime switch: the detached child claims a control-store job, reads/decrypts bounded evidence, invokes the isolated provider, validates the Task 7 result, then commits only `reviewed_no_lesson` or a Task 8 immutable Markdown path/hash. Stable document timestamps are `source.source_timestamp ?? source.created_at` for `createdAt` and durable `job.created_at` for `publishedAt`; retry-time/`claimed_at` clocks are forbidden so crash adoption renders identical bytes. The provider comes from captured `source_provider`, `project_id` must be an absolute owned project root, all providers use the private `0600` Task 7 result handoff, logs remain opaque, and prompt launch/recovery are bounded synchronous spawn attempts with no scheduler or main-session wait. `src/index.mjs` is added only to repoint the installed schema filename to `reviewer-result.schema.json`.
- Task 9 implementation commit `1497203` atomically replaces the old receipt runner/provider path with a control-store-only runner, private result-file provider protocol, immutable Markdown terminal publication, immediate detached launch and one neutral-prompt-capable bounded recovery. The old reviewer auth/schema/test and installed command/stdout-receipt rule text are removed together. Implementer RED was 93 pass / 5 expected failures; coordinator reran the frozen eight-file regression at 142/142 and the current full suite at 297/297. Native macOS disposable-HOME tests exercised a real detached child with a fake local provider, stdin-only evidence, one new job plus one older recovery and no hook stdout/control leakage. No real HOME, hooks, runtime pointer, provider account or production database was touched. Formal review uses stable Review-Run-ID `isolate-feedback-control-plane-task-9-v2-review-1` against package `.superpowers/sdd/review-b21a6bb..1497203.diff`.
- Task 9 formal review `isolate-feedback-control-plane-task-9-v2-review-1` returned CHANGES_REQUIRED with Critical 0 and Important 2. The stable loops are `reviewer-context-credentials-never-reach-provider` at `reviewer-runner/context-redaction` (supported English/Chinese/reminder credentials reached provider context) and `publication-owner-valid-at-visibility-fence` at `reviewer-runner/reflection-publication-fence` (deterministic probe left one canonical file after the lease expired between callback assertion and link). The first outdated Guard attempt exited 2 with `review_evidence_required` and made no state change. After the same reviewer added hypothesis/new-evidence/falsification/next-action fields, both canonical `record-review` calls exited 0 with failure_count 1; two generation-1 local-fix receipts were authorized, persisted, and consumed by separate fix briefs. The next failed review of either invariant must enter direction review; no second local fix, count reset or ID replacement is permitted.
- Task 9 guarded fix commit `f1bc209` changes only `src/reviewer-runner.mjs` and `test/reviewer-runner.test.mjs`: the runner now reuses the capture-layer redaction contract, and the Task 8 visibility callback renews the exact owner/epoch lease. Fix RED was 7/12 with five expected failures, then focused runner 12/12, frozen Task 9 regression 147/147 and current full suite 302/302 passed. Fresh re-review `isolate-feedback-control-plane-task-9-v2-rereview-1` independently passed the three credential and two publication-fence probes at 5/5 and returned APPROVED with all finding counts zero. Both Guard loops closed while retaining failure_count 1 and generation-1 history; Task 9 is frozen complete. Evidence remains disposable/native macOS only, with Linux and real provider/account/live installation deferred to Task 15.
- Task 10 preflight freezes the lean direct-document boundary: Task 8's existing bounded no-follow catalog read adds the SHA-256 of those exact bytes, and selector code only projects that result, maps the ten public omission reasons, performs lexical/metadata relevance, newest-applicable family projection, future emission suppression, exact total ordering and bounded guidance. The prompt hook uses its already-frozen cutoff and native `hookSpecificOutput.additionalContext`; empty/failing selection remains a no-op. The explicit legacy `memory list` store call is allowed only until Task 13 and is excluded from the prompt runtime scan. No second file read, cache, index, RAG, schema, service, scheduler, real hook or user-state change is in scope. Baseline on the real macOS arm64 host with Node v26 passed the three focused files at 44/44.
- Task 10 implementation exposed one pre-review plan omission: after replacing `src/selector.mjs`, `test/store.test.mjs` could not load because it still imported the rejected legacy selector solely for one `memory_overflow_hold` case. The bounded pre-review correction removes only that import and already-replaced hold test so the remaining legacy store invariants continue running until Task 13; it does not restore a compatibility selector or delete the store early.
- Task 10 formal review `isolate-feedback-control-plane-task-10-v2-review-1` returned CHANGES_REQUIRED with Critical 0, Important 1 and Minor 0. Stable invariant `t10-rank-before-document-budget` at `selector/rank-to-per-document-budget` proves the implementation emitted per-document token omissions in stable identity order before applying the frozen rank tuple. Guard `record-review` exited 0 with failure_count 1; generation-1 `local_fix` receipt `014fa8a9fb0567bb2daa761d5b701034f3c020a12934e4fdb6642bf0b4c182d5` is persisted at `.superpowers/sdd/isolate-feedback-control-plane-task-10-fix-rank-before-budget.guard-receipt.json` and was consumed by `.superpowers/sdd/isolate-feedback-control-plane-task-10-v2-fix-1-rank-before-budget-brief.md`. Any second failed review of this invariant must enter direction review; no second local fix, reset or replacement ID is permitted.
- Task 10 guarded fix commit `41c7ea7` changes only `src/selector.mjs` and `test/selector.test.mjs`: projected non-prior-emitted documents now receive the unchanged exact rank before guidance construction and per-document budgeting. The fixer observed the opposing identity/rank fixture RED at 0/1, then 1/1 GREEN, selector 11/11, frozen Task 10 44/44, legacy store 56/56 and full 301/301. The coordinator independently observed the same key fixture 1/1, Task 10 plus store 100/100 and full 301/301 on macOS arm64 Node v26.
- Fresh re-review `isolate-feedback-control-plane-task-10-v2-rereview-1` approved the original invariant with Critical 0, Important 0 and Minor 0; Linux, live provider and real desktop/hook acceptance remain the three explicit Task 15 verification boundaries. Guard status is closed while retaining failure_count 1 and generation-1 history. Task 10 is frozen complete; no additional Task 10 review/fix round is open.
- Task 11 preflight reuses the existing schema-v1 emission ledger without migration. The exact application identity remains `(document_sha256, session_uid, context_epoch, task_fingerprint)`; only emitted rows suppress same-scope reinjection, while selected-only attempts remain retryable. The prompt path records selection before one host write and emission only after success; audit failures underclaim and never block or retry the host response. Reviewer recurrence requires a same-family emission strictly before the source captured timestamp and uses the catalog's already-read exact-byte hash. The affected control-plane surface permits only `selected`, `emitted`, `unknown`, and `recurrence_after_emission`; legacy state vocabulary remains isolated until Task 13. Baseline for the four focused files is 123/123 on macOS arm64 Node v26.
- Task 11 implementation commit `41e32fd` changes exactly the frozen four production and four test files with no schema change. It records selected before one host writer, marks emitted only after resolution, suppresses only exact emitted tuples, preserves first emission time, and classifies only strict pre-source same-family emission as `recurrence_after_emission`. Implementer evidence is RED 0/5 plus supplemental 0/3, GREEN 5/5 plus 3/3, affected 131/131 and full 309/309. Coordinator independently observed the combined key boundary suite at 8/8, an empty forbidden-state scan, and strict OpenSpec validation.
- Formal review `isolate-feedback-control-plane-task-11-v2-review-1` returned APPROVED with Critical 0, Important 0, Minor 0 and Backlog 0. Its novel disposable probe passed exact tuple isolation, selected-only retryability, and timezone-normalized strict-before behavior without touching real HOME or runtime state. With no Critical/Important invariant, the Guard correctly opened no finding or receipt. Task 11 is frozen complete; Linux/live host transport and real installation remain Task 15 boundaries.
- Task 12 preflight freezes an offline-only exporter: exact legacy v8/v9 copies, no-follow source-set hashing and private snapshot, read-only fixed SELECTs over only the four declared tables, deterministic per receipt/lesson/revision conversion, opaque ids, dry-run with zero output creation, Task 8 atomic apply, idempotent skip/collision accounting, and an unconditional apply refusal for the canonical live legacy DB. Normal runtime gains no legacy read path and no migration, RAG, scheduler, service, hook or provider behavior. Baseline `test/cli.test.mjs test/reflection-document.test.mjs` is 38/38 on macOS arm64 Node v26.
- Task 12 implementation commit `049c598` adds the explicit offline exporter and strict CLI in exactly four scoped files. Initial RED was 23/26 with only the missing module/command failures; focused GREEN reached 50/50 and full regression 321/321. Native disposable macOS CLI smoke proved dry-run creates no output, apply writes one 0600 Markdown file beneath a new 0700 leaf, and the source DB/WAL/SHM hashes remain unchanged. Formal review `isolate-feedback-control-plane-task-12-v2-review-1` returned one Important invariant, `T12-EVIDENCE-LESSON-ROW-COHERENCE` at `legacy-export/evidence-row-coherence`: an exact-current receipt/lesson row mismatch could still be planned. Guard record/authorize/consume all exited 0 and authorized exactly one generation-1 local fix.
- Task 12 fix commit `8954176` changes only `src/legacy-export.mjs` and `test/legacy-export.test.mjs`: exact-current severity/responsibility/method/class mismatches now become opaque `mismatched_lesson`, while later-current rows preserve valid historical receipt export. The fixer and coordinator independently observed focused 62/62 and full 333/333. Re-review `isolate-feedback-control-plane-task-12-v2-rereview-1` was calibrated against the frozen invariant after rejecting a proposed reason-precedence expansion, then returned APPROVED with Critical 0, Important 0, Minor 0 and Backlog 0. Guard closed the original loop with failure_count 1 and generation-1 history intact. Task 12 is frozen complete; no runtime legacy read path, real HOME/database mutation, scheduler, RAG, hook or provider activation was introduced.
- Task 13 preflight found the final legacy runtime consumers by direct import scan: `src/index.mjs` still imports the legacy schema version, `src/capture.mjs` and `src/feedback-signal.mjs` still import the receipt stripper, and `src/cli.mjs` still imports `openStore()` for live doctor plus four obsolete command families. The frozen boundary therefore includes the necessary CLI deletion/switch instead of leaving a broken binary. Existing control-store tests already cover replay/races, encrypted refs, invalid capture rejection, bounded context, lease fencing and body-free schema; Task 13 adds only private-create modes, transactional rollback and non-default WAL reopen proofs. The Task 12 CLI fixture remains an allowed third old-table-name test file. Baseline for capture/control-store/runtime/CLI/E2E is 158/158 on macOS arm64 Node v26; no real HOME, hooks, runtime pointer or database was touched.
- Task 13 implementation `d055993` removes the legacy store/schema/receipt/lesson runtime and obsolete CLI command families, makes capture control-store-only, transfers the bounded synthetic-control parser, preserves only the explicit offline exporter, and leaves the normal import graph body-free. Formal review `isolate-feedback-control-plane-task-13-v2-review-1` found two Important proof/deletion gaps: dormant Stop normalization and a test-authored rather than production transaction rollback. Both generation-1 local-fix receipts were recorded, authorized, persisted and consumed before fix `f8c9773`; fixed-scope re-review `isolate-feedback-control-plane-task-13-v2-rereview-1` approved both original invariants and the parser-matrix Minor. Guard retains failure_count 1 for each closed loop. Coordinator evidence is focused 3/3, affected 143/143, E2E 9/9, full 258/258, empty forbidden scans, strict three-file legacy-table scan, and native disposable macOS doctor proof; Linux/live provider/desktop installation remain Task 15 boundaries.
- Task 14 preflight freezes a compact `{ version, status }` doctor contract with exactly six status families, stderr-only allowlisted JSON events, one exported/package runtime version `0.8.0`, and concise bilingual documentation of only the implemented prompt/Markdown pipeline. The old E2E live-doctor consumer is included in Task 14 so no compatibility health shape survives. Baseline is 44/44 for runtime/CLI/E2E plus a successful `npm pack --dry-run --json` containing 31 files at version `0.7.6`; no real HOME or provider was touched.
- Task 14 implementation `5451799` introduced the six-family doctor, bounded JSONL diagnostics, runtime/package version `0.8.0`, prompt-only bilingual documentation, and exact package inventory. Formal review `isolate-feedback-control-plane-task-14-v2-review-1` found four Important invariants: readiness omitted declared dependencies, `family` could leak content, failed launch was reported as successful, and the fixed event vocabulary lacked a truthful post-claim reviewer failure event. The first three received generation-1 local-fix receipts; the fourth returned Guard exit 3 with `structural_blocked`, then a direction checkpoint authorized one architecture fix adding only `review_failed`. Fix `d14731c` corrected all four without adding storage, transport, service, scheduler, schema, Stop, receipt, notification, or Task 15 behavior. Coordinator evidence is targeted 50/50, full 264/264, pack 0.8.0 with 31 files and no removed artifacts, plus real macOS disposable-HOME install/doctor-live with six keys and `ready=true`. Frozen re-review `isolate-feedback-control-plane-task-14-v2-rereview-1` returned APPROVED C0/I0/M0/B0; all four Guard loops are closed with failure history retained. Linux, live-provider, and desktop acceptance remain Task 15 or later user-authorized boundaries.
- Task 15 preflight fixes one vertical acceptance harness rather than adding runtime machinery. Default mode uses a deterministic disposable provider but real OS process/filesystem/SQLite paths; `AFL_REAL_PROVIDER=1` is a separate macOS acceptance path that must invoke the locally installed provider and cannot be satisfied by a fake. The local Docker Linux engine is the declared real Linux container executor with a read-only repository mount and disposable writable HOME. Before any Task 15 implementation command, the coordinator recorded read-only hash/size/mtime evidence for the real Codex config, managed runtime pointer, and legacy DB/WAL/SHM; AFL did not open them. The frozen exclusions remain scheduler, RAG, service, schema, legacy control output, Windows, real AFL installation/migration, and desktop UI claims.
- Real Codex desktop installation/visibility remains a later user-authorized verify activity, separate from temporary-HOME build proof.
- Historical review note: Task 1 previously stopped at the configured review ceiling on three concrete contract gaps. Those gaps and later bounded findings were resolved under explicit user-authorized rounds; this historical gate is superseded by the final frozen acceptance PASS.
- On 2026-07-17 the user explicitly replied `继续`, authorizing one third-round exception limited to those three gaps; the task remains unchecked until a fresh reviewer approves it.
- The exception review closed alias truncation and duplicate-result propagation but found two Task 1 blockers: incomplete generated-column/index-collation schema inspection and missing provider isolation in alias/explicit-target identity. A fourth round requires a new explicit user decision.
- The user replied `继续` again on 2026-07-17, authorizing a fourth-round exception limited to those two blockers; no task is checked until a fresh reviewer approves.
- Review 5 confirmed the fourth-round xinfo/provider fixes but found two different blockers: identical same-provider alias replays collide, and undeclared `CHECK`/trigger semantics are not covered by the schema fingerprint. A fifth targeted fix/re-review requires a new explicit user decision.
- The user replied `继续` on 2026-07-17, authorizing a fifth-round exception limited to those two deterministic blockers; no task is checked until a fresh reviewer approves.
- Review 6 confirmed the fifth-round alias/CHECK/trigger fixes but found two identity blockers: public duplicate replay omits persisted immutable referent/source/completeness fields, and a shared session UID can rewrite its provider. Undeclared VIEW acceptance is Minor. A sixth targeted fix/re-review requires a new explicit user decision.
- The user replied `继续` on 2026-07-17, authorizing a sixth-round exception for both identity blockers and the adjacent canonical-schema VIEW gap; no task is checked until a fresh reviewer approves.
- Review 7 closed the review-6 provider/VIEW findings but proved the identity model is still fragmented: public capture drops capture source while direct replay bypasses observation-signature checks. After repeated field-level fixes, the next attempt must unify normalization/signature/equality/replay around one canonical capture identity contract, not append another isolated field check; this requires a new explicit user decision.
- Review 9 confirms the atomic frozen-snapshot redesign closes all three review-8 blockers, but found one new bounded Important: public capture does not reject a null/undefined blob-writer result before entering the direct-only nullable authoritative-ref path. The authorized implementation/re-review round is consumed; no fix is dispatched until the user explicitly authorizes one bounded fix and fresh re-review.
- The user replied `继续` after review 9, authorizing exactly one bounded fix for the public null-writer invariant and one fresh independent re-review; the direct-only nullable path remains unchanged.
- Review 10 approves the public writer-ref fix but finds two deterministic identity-contract blockers: alias attachment can create a completeness-mismatched observation that exact replay rejects, and most snake/camel canonical identity conflicts are silently resolved by precedence. The authorized review-9 fix/re-review round is consumed; no further fix is dispatched without a new explicit user decision.
- On 2026-07-20 the user replied `继续`, authorizing one review-10 identity-coherence fix and one fresh independent re-review. The fix must use a shared conflict-validating reader for all existing canonical alias groups and a shared replay-compatible alias-target rule with completeness applied before candidate bounding; no schema, hook, scheduler, notification, RAG, Markdown-truth, or later-task expansion is authorized.
- Review 11 confirms both review-10 blockers are closed but proves one remaining semantic split: SQLite `julianday()` accepts a timezone-less timestamp as a 60-second alias match while JavaScript `Date.parse()` sees a 28,740-second difference, so the same accepted alias immediately collides on replay. The authorized fix/re-review round is consumed; no timestamp-contract fix is dispatched without a new explicit user decision.
- On 2026-07-20 the user replied `继续`, authorizing one root-cause timestamp-contract amendment, its TDD implementation, and one fresh independent review. The correction must normalize valid timezone-bearing RFC3339 input to fixed-width UTC before side effects and make SQL/replay consume one canonical inclusive window; it must not add schema, services, schedulers, hooks, notifications, RAG, or later-task behavior.

## Convergence Kernel Guard Integration (2026-07-21)

- Plan: `docs/superpowers/plans/2026-07-21-convergence-kernel-guard-integration.md`
- Design: `docs/superpowers/specs/2026-07-21-convergence-kernel-guard-integration-design.md`
- Task 1: complete (commits `ba0e087`, `5279d98`, `0d625ef`; frozen human-resolution review clean with Critical 0 / Important 0 / Minor 1). Both Guard loops are closed while retaining failure counts 2 and 1 plus one architecture-fix generation; the binding decision is JSON value semantics, with property descriptor flags non-semantic. Minor retained for final triage: negative lineage safety and concurrent-creation coverage.
- Task 2: complete (commits `2a22080`, `71ec9be`; frozen re-review clean with Critical 0 / Important 0 / Minor 1). Guard invariant `CONV-POLICY-ROUTINE-EVIDENCE-FREE-REFLECTION` is closed with failure count 1 retained. Verification: policy 24/24, Task 1+2 38/38, full suite 310/310 on the current macOS Node runtime. Minor retained for final triage: tail trigger-priority collision coverage. Cross-task acceptance retained for Tasks 3/6: the Store/controller must bind `breaker_triggered` transitions to the already evaluated decision.
- Task 3: complete (commits `b1d96bd`, `3f68a46`, `0bc8e12`; final frozen re-review clean with Critical 0 / Important 0 / Minor 0 / Backlog 0). Five first-review invariants closed: decision, Probe, and import retained failure count 1/local-fix generation 1; loop history and event replay retained failure count 2 plus the single architecture-fix generation. The direction checkpoint replaced duplicate review identity paths with one canonical review envelope; no schema/runtime expansion. Verification: Task 3 three-file regression 122/122, full suite 338/338, disposable macOS atomic/concurrency/replay smoke passed. Computer Use Terminal remained host-policy blocked, so no UI proof is claimed.
- Task 4: complete by frozen review plus explicit human resolution (commits `58e2f13`, `57ba8a0`, `6374194`). Four Guard loops closed after one local-fix generation; artifact lifecycle closed with failure count 2 and one architecture-fix generation after the user accepted the descriptor-bound implementation and excluded hostile same-UID temp-source replacement from the supported threat model. Verification: Task 4 44/44, Task 3+4 167/167, Python Guard oracle 29/29, full suite 353/353, disposable macOS two-repository/inode/no-clobber/tombstone workflow passed. Backlog retained: constant `lock-status`, legacy `status`/`resolve` surface differences, and same-UID temp-source replacement only if real production evidence appears.
- Task 5: complete (commits `8e3bf4b`, `d1ef68c`; architecture re-review clean with Critical 0 / Important 0 / Minor 0 / Backlog 0). Guard invariant `probe-advice-never-directly-advances-hard-state` is closed with failure count 1 and one architecture-fix generation retained. Probe output keeps the exact seven-field/six-action advisory contract, while every successful completion now persists only immutable action/result evidence and neutral `reflection_resolved`; deterministic Task 6 policy exclusively owns checkpoint, human-decision, terminal, and grant transitions. Verification: focused RED 0/1 then GREEN 1/1, Task 5 37/37, Task 3 124/124, affected 149/149, full suite 369/369 on disposable macOS state. Linux and live-provider evidence remain later acceptance boundaries.
- Task 6: complete (commits `8a03391`, `45d3c74`, `ce28ed2`; final frozen re-review clean with Critical 0 / Important 0, retained Minor 1 / Backlog 1). All five Guard invariants are closed with original fingerprints and history retained: evidence provenance, restart-safe post-Probe transition, explicit-invocation Probe recovery, review-to-decision fail-closed atomicity, and controller-owned canonical grant scope. Policy revision v2 and revision-specific task projection revoke stale grants without schema change; detached Probe remains advisory and neutral, while the existing explicit SDD path reconstructs immutable completion/review evidence after restart and applies the sole deterministic policy before hard state or one-shot grant. Verification: final Task 6 77/77, Task 4 Node parity 49/49 plus Python oracle 29/29, affected 246/246, full suite 397/397, and disposable native macOS detached/restart/authorization/idempotent-replay smoke. Retained Minor: a materially different second unconsumed exploration request fails closed with `invalid_transition` instead of a stable budget-exhausted action. Retained Backlog: stale caller task projection may affect synchronous pass/warn output, with no demonstrated authority bypass. Linux remains contract-tested only; live-provider and production acceptance remain later boundaries.
- Task 7: complete (architecture-boundary correction `04a1837`, implementation `694cd96`, local fix `fa6595b`; final frozen re-review clean with Critical 0 / Important 0 / Minor 0 / Backlog 0). A pre-implementation checkpoint corrected the single-task Store overfit without adding a fifth table: one lineage/source/mapping transaction imports all real legacy task groups under exactly one provenance event, and Store-owned shadow/cutover/rollback events remain the sole authority. The three review invariants are closed with failure count 1 and generation-1 local-fix history retained: legacy Review-Run-ID replay identity, semantic four-field shadow parity, and exact immutable-snapshot rollback. The orchestrator's first `record-review` calls used lowercase `important`; the Guard retained those three non-counted review events and rejected authorization with exit 6. No state was reset or edited: a fresh independent review with exact `Important` supplied new evidence, then the normal record/authorize/consume path issued the three receipts used by the single fixer. Verification: focused 66/66, CLI 31/31, Python Guard oracle 29/29, full suite 415/415, final reviewer-focused 6/6, and disposable native macOS package probes for imported review replay/collision, invalid parity rejection, and drifted-target rollback. The real Guard file, global Skill, and real AFL database were not mutated; Linux remains contract-tested only and no real import/cutover occurred.
- Task 8: complete (architecture correction `2e788ba`, implementation `65b0f27`; frozen review clean with Critical 0 / Important 0 / retained Minor 1 / Backlog 0). The checkpoint proved the planned automatic effectiveness-to-Markdown path had no real producer, evidence envelope, independent learning-job authority, or source-compatible lesson contract; implementing it would have conflated decision Probe state or fabricated session evidence. The current task therefore contains only pure capability-bounded OpenSpec, Comet, and generic projections in two files. Approved matching native/revision/authority inputs may expose `checkpoint_gate`; every incomplete, stale, mismatched, inferred, or generic input downgrades truthfully, and generic remains `audit_only`/`warn`. Existing real dissatisfaction events remain the sole automatic background reviewer-to-Markdown producer; independent Guard-outcome learning is deferred behind named-producer/evidence/job-contract acceptance. Verification: focused 11/11, affected 72/72, full suite 426/426, disposable macOS direct-import smoke, and reviewer sparse/decorated in-memory probe. Retained Minor: no committed test case names the sparse-array rejection explicitly, although implementation behavior was independently verified. Linux remains contract-tested only.
- Task 9: pending.
- Final whole-branch review: pending.
